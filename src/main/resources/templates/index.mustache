<!--
Mustache 파일 위치 : (기본) src/main/resources/templates
- 이 위치에 mustache 파일을 두면 스프링부트에서 자동으로 로딩
-->

<!-- 변경 후 코드
- 라이브러리 및 HTML 태그들이 모두 레이아웃에 추가된다.
- 이제, index.mustache에는 필요한 코드만 남게 된다.
-->
{{>layout/header}}
    <h1>스프링부트로 시작하는 웹 서비스 Ver.2</h1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <!--글 등록 버튼, 이동할 페이지 주소는 /posts/save-->
                <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
            </div>
        </div>
        <br>
            <!-- 목록 출력 영역 -->
        <table class="table table-horizontal table-bordered">
            <thead class="thead-strong">
            <tr>
                <th>게시글번호</th>
                <th>제목</th>
                <th>작성자</th>
                <th>최종수정일</th>
            </tr>
            </thead>
            <tbody id="tbody">
            <!--
            #posts
            - posts라는 List를 순회
            - Java의 for문과 유사하다.

            중괄호 2개 변수 (id, title, author, modifiedDate)
            - Lists에서 뽑아낸 객체의 필드를 사용


            -->
            {{#posts}}
                <tr>
                    <td>{{id}}</td>
                    <td>{{title}}</td>
                    <td>{{author}}</td>
                    <td>{{modifiedDate}}</td>
                </tr>
            {{/posts}}
            </tbody>
        </table>
        </br>
    </div>

{{>layout/footer}}


<!-- 변경 전 코드
<!DOCTYPE HTML>
<html>
<head>
    <title>스프링부트 웹 서비스</title>
    <meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
</head>

<body>
    <h1>스프링부트로 시작하는 웹 서비스</h1>
</body>


</html>
-->

